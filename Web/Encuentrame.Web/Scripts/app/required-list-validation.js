(function () {
    //$.validator.setDefaults({
    //    ignore: 'input[type=hidden]'
    //});
    //var validator = $('#form').validate({ ignore: null });
    //Tell the validator to only ignore inputs of type 'hidden'
    //var validator = $('form').validate({ ignore: 'input[type=hidden]' });

    //$.validator.addMethod("requiredlist", function (value, element, params) {
    //    return false;
    //});

    //$.validator.unobtrusive.adapters.add("requiredlist", function (options) {        
    //    options.messages["requiredlist"] = options.message;
    //});
})();